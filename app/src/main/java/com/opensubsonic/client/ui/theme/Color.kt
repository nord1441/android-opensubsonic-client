package com.opensubsonic.client.ui.theme

import androidx.compose.ui.graphics.Color

// Dark theme palette — deep pure black base
val TermBlack = Color(0xFF0a0a0a)       // Pure deep black background
val TermDarkBg = Color(0xFF141414)      // Slightly lighter bg for surfaces
val TermSurfaceDark = Color(0xFF1e1e1e) // Surface variant
val TermBorder = Color(0xFF2a2a2a)      // Borders and outlines
val TermMuted = Color(0xFF666666)       // Muted text / inactive
val TermSubtle = Color(0xFF888888)      // Subtle text
val TermFg = Color(0xFFe0e0e0)         // Primary foreground text (high contrast)
val TermBright = Color(0xFFffffff)     // Bright foreground — pure white

// Accent colors — true red
val TermRed = Color(0xFFe01020)         // Primary accent — pure red (active/selected)
val TermRedDim = Color(0xFF7a1018)      // Dark red (inactive/unselected buttons)
val TermRedContainer = Color(0xFF2a0a0e) // Very dark red container bg
val TermCyan = Color(0xFF7dcfff)        // Info accent
val TermGreen = Color(0xFF9ece6a)       // Success
val TermYellow = Color(0xFFe0af68)      // Warning
val TermMagenta = Color(0xFFbb9af7)     // Tertiary accent
val TermOrange = Color(0xFFff9e64)      // Secondary warm accent

// Light theme — clean pure white base
val LightBg = Color(0xFFffffff)         // Pure white background
val LightSurface = Color(0xFFf5f5f5)    // Surface
val LightSurfaceVar = Color(0xFFeeeeee) // Surface variant
val LightBorder = Color(0xFFcccccc)     // Borders
val LightFg = Color(0xFF111111)         // Near-black text (high contrast)
val LightMuted = Color(0xFF555555)      // Muted text
val LightRed = Color(0xFFc41010)        // Accent on light (active) — true red
val LightRedDim = Color(0xFF7a1018)     // Dim red on light (inactive)
val LightRedContainer = Color(0xFFf5d5d5) // Light red container
